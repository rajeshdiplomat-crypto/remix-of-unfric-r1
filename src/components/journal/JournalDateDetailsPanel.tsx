import { JournalSkin } from "./types";

interface JournalDateDetailsPanelProps {
  selectedDate: Date;
  wordCount: number;
  streak: number;
  skin: JournalSkin;
}

// Placeholder component - can be expanded later
export function JournalDateDetailsPanel({
  selectedDate,
  wordCount,
  streak,
  skin,
}: JournalDateDetailsPanelProps) {
  return null;
}
